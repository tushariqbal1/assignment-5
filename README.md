1--> What is the difference between var, let, and const?
Ans: Var is a function-scoped, and var is re declarable, and let & const are blocked-scoped let are reassignment, and const don't.

2--> What is the spread operator (...)?
Ans: spread operator (...) is javaScript ES6 method which is work for spreads or extracts the element within array or object or string.

3--> What is the difference between map(), filter(), and forEach()?
Ans: In javaScript map(), filter() and forEach() are all for array, but their main difference are their return values and purpose, map() returns a new array, filter() create a new array, and forEach() does not return anything because it is a loop.

4-->  What is an arrow function?
Ans: arrow (=>) function is a modern way to write function in javaScript (ES6).

5--> What are template literals?
Ans: template literals is a javaScript string in (ES6) that allow for multi line writing and make variable expression directly inside the string using (``) this.